{
  "name": "portfolito",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "lucide-react": "^0.363.0",
    "pdfjs-dist": "^4.0.379",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && serverless client deploy --no-confirm",
    "remove": "serverless client remove --no-confirm"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "serverless": "^4.33.3",
    "serverless-finch": "^4.0.4"
  }
}
